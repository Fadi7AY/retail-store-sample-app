apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: retail-store
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ui-chart
            port:
              number: 80
